import { AcademicsView } from '@/components/academics/AcademicsView';

export const metadata = { title: 'Academics — Studer' };

export default function AcademicsPage() {
  return <AcademicsView />;
}
